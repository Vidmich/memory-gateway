.DS_Store
.git/
.gitignore
*.tmproj
*.tgz
# Example overrides live beside the chart for readability but must not be packaged into
# it: a values file inside a chart is one somebody can apply by accident.
../examples/
